# 핵심 파이프라인(수집 폴백 → 규칙 기반 요약 → Stooq/합성 시세 → 정적 HTML)은
# 표준 라이브러리만으로 동작합니다. 아래는 "라이브 모드" 선택적 의존성입니다.

# 텔레그램 채널 수집 (MTProto). 봇 관리 권한이 없어도 개인 세션으로 구독 가능.
telethon>=1.36,<2

# Claude 기반 시황/종목 구조화 요약
anthropic>=0.40
